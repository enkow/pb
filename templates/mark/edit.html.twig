{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.mark_edit'|trans({'%id%': mark.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.mark_edit'|trans({'%id%': mark.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('mark_edit', {id: mark.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('mark_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}