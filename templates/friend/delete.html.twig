{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.friend_delete'|trans({'%id%': friend.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.friend_delete'|trans({'%id%': friend.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('friend_delete', {id: friend.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('friend_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}